import { TeorAlcoolicoPipe } from './teor-alcoolico-pipe';

describe('TeorAlcoolicoPipe', () => {
  it('create an instance', () => {
    const pipe = new TeorAlcoolicoPipe();
    expect(pipe).toBeTruthy();
  });
});
